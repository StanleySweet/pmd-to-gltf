# Animation speed overrides (percent). 100 = original timing.
# Use 'default=' to set a base speed for any not listed.
# Example values below; adjust as needed.

default=100
idle=50
idle_a=60
walk=40
gallop=240
attack_a=100
attack_b=100
death=80
